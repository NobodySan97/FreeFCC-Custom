name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Assemble release
        run: ./gradlew assembleRelease --no-daemon

      - name: Run unit tests
        run: ./gradlew testDebugUnitTest --no-daemon

      - name: Run Android lint
        run: ./gradlew lintDebug --no-daemon

      - name: Upload unsigned APK
        uses: actions/upload-artifact@v4
        with:
          name: release-unsigned-apk
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error

  signed-release:
    if: github.event_name == 'workflow_dispatch'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    env:
      SIGNING_STORE_FILE: ${{ github.workspace }}/release.jks
      SIGNING_KEYSTORE_B64: ${{ secrets.SIGNING_KEYSTORE_B64 }}
      SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
      SIGNING_KEY_ALIAS: ${{ secrets.SIGNING_KEY_ALIAS }}
      SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
      FREEFCC_STATISTICS_ENDPOINT: ${{ secrets.FREEFCC_STATISTICS_ENDPOINT }}
      FREEFCC_STATISTICS_ENDPOINT_SECONDARY: ${{ secrets.FREEFCC_STATISTICS_ENDPOINT_SECONDARY }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Check signing secrets are present
        id: secrets
        run: |
          if [ -z "$SIGNING_KEYSTORE_B64" ] || [ -z "$SIGNING_STORE_PASSWORD" ] || [ -z "$SIGNING_KEY_ALIAS" ] || [ -z "$SIGNING_KEY_PASSWORD" ] || [ -z "$FREEFCC_STATISTICS_ENDPOINT" ]; then
            echo "have_secrets=false" >> "$GITHUB_OUTPUT"
            echo "Warning: signing or statistics endpoint secret not configured — skipping signed build." >&2
          else
            echo "have_secrets=true" >> "$GITHUB_OUTPUT"
            if [ -z "$FREEFCC_STATISTICS_ENDPOINT_SECONDARY" ]; then
              echo "Warning: secondary statistics endpoint not configured — reports go to one host only." >&2
            fi
          fi

      - name: Decode keystore
        if: steps.secrets.outputs.have_secrets == 'true'
        run: echo "$SIGNING_KEYSTORE_B64" | base64 --decode > "$SIGNING_STORE_FILE"

      - name: Assemble signed release
        if: steps.secrets.outputs.have_secrets == 'true'
        run: ./gradlew assembleRelease --no-daemon

      - name: Upload signed APK
        if: steps.secrets.outputs.have_secrets == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: release-signed-apk
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error
